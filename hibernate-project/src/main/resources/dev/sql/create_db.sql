DROP TABLE HIBERNATE.T_CAR;
DROP TABLE HIBERNATE.T_ID_GENERATOR;

CREATE TABLE HIBERNATE.T_CAR (
  ID								    NUMBER(19),
  NAME								    VARCHAR2(100 CHAR),
  DATE_ID							    VARCHAR2(100 CHAR),
  CONSTRAINT PK_CAR PRIMARY KEY (ID)
);

CREATE TABLE HIBERNATE.T_ID_GENERATOR (
	ID_GENERATOR 									VARCHAR2(100 CHAR),
	COUNT											NUMBER(19),
	CONSTRAINT PK_ID_GENERATOR PRIMARY KEY (ID_GENERATOR)
);

INSERT INTO T_ID_GENERATOR(ID_GENERATOR, COUNT) VALUES('T_CAR_GENERATOR',1);
COMMIT;